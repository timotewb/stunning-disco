



- daily notes and markdown editor, a tab where the user can enter notes in markdown format and optionally preview them in real time. these notes are saved to the same location as the sqlite database, in a directory `notes/yyyymm/yyyy-mm-dd.md`. the user can create a new note for the day by clicking a button (or be taken to todays note if already exists) and browse and key word search through all note files.
- data import and export in settings where all database 